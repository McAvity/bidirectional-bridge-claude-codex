{
  "format": "claude-codex-bridge.release/v1",
  "package_version": "0.3.2",
  "pinned": {
    "commit": "41962cce73b01db8c20712e946dce6b46e67cb10",
    "describe": "0.3.1: Codex 0.155.1 compatibility; unverified host versions warn instead of refusing"
  },
  "repository": "https://github.com/McAvity/bidirectional-bridge-claude-codex.git",
  "notes": [
    "This file is a source, not a generated artefact: the generator copies it verbatim so a",
    "release pin does not change every time the repository moves. Update it deliberately when",
    "cutting a distribution, and only to a commit that actually exists.",
    "Acquisition order is: an explicit --source, then CLAUDE_CODEX_BRIDGE_SOURCE, then a cached",
    "clone under <home>/sources/, then a fetch of exactly this commit from `repository`. Every",
    "path verifies the resolved commit hash before anything is installed, so no unpinned HEAD is",
    "ever built.",
    "No npm publish is involved and a marketplace is never assumed to install dependencies: the",
    "wave12 installer runs `npm ci --ignore-scripts` and `npm run build` into an immutable runtime."
  ]
}
