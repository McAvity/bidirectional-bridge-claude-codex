# Ledger W14-01 / attempt 02 — resumed round: packaging and close-out

## Scope / Inputs

Same task and authority as [attempt 01](01.md), which is preserved unchanged:
`work-items/W14-01.md` (`d3286997…8ee9`) under
`docs/features/F-W14-plugin-distribution/decisions/01.md` (`66c488ff…439b0`),
bridge round executor mode.

This attempt resumes after the previous one stopped at its turn limit. Per the
manager clarification for this recovery, the completed work is kept and not
redone: the host probes and the two proposed contracts stand as committed in
`dd1e694a801d290c509bdd83c2c684221d94e3d8`, and the contracts remain proposals
for separate coordinator review. No further product scope is authorised in this
round.

Starting state inspected before any work: branch `wave14`, working tree clean,
`HEAD = dd1e694a801d290c509bdd83c2c684221d94e3d8`, round base
`1f7d34f1d3bf1de15c2b9cef5b3b121f2c8c27c0`.

## Outcome

`complete`. The round's outstanding item was packaging and the final report.

Delivered by this attempt: this ledger and a verified round package exported
under a non-colliding name. Nothing else changed — no probe, contract, evidence
file or test was edited.

## Problems and resolutions

**An archive from the interrupted attempt already existed.** The previous attempt
had exported and verified
`~/tmp/bridge-exchange/ws_34ec282468d2d55d/packages/F-W14-round-1.zip`
(`sha256 960cf505…c523`, range `1f7d34f…dd1e694`, integrity `ok`, 17 code
changes). It was inspected rather than overwritten, and this attempt exports to
`F-W14-round-1-resumed.zip` so the earlier package stays intact as historical
evidence. The resumed package uses the same base and purpose and a head that
additionally contains this ledger.

## Validation

Run on the final snapshot of this attempt, from the worktree root.

| Check | Command | Result |
| --- | --- | --- |
| Probe fixtures | `python3 -m unittest discover -s tests/plugin-probes` | 19 tests, OK, exit 0 |
| Whitespace | `git diff --check` | clean, exit 0 |
| Package integrity | `feature_exchange.py verify --archive …/F-W14-round-1-resumed.zip` | `integrity: ok`, `code_range_matches_repository: true`, exit 0 |

**Host probe evidence is retained, not re-executed.** The four model-free probes
were run in attempt 01 and their results are committed under
`evidence/W14-01/results/`. The tested revision of the probe sources is
`dd1e694a801d290c509bdd83c2c684221d94e3d8`
(`scripts/plugin-probes/`), and those sources are byte-identical at this
attempt's head; only this ledger was added. The last full run
(`python3 scripts/plugin-probes/run_probes.py --out-dir …`) exited 0 with every
derived finding in all four results reproducing. Re-running them here would spend
host time without testing anything that changed.

**Still not run:** `npm ci --ignore-scripts`, `npm run build`, `npm test`. This
worktree has no `node_modules`, and neither attempt changed TypeScript, a build
input or runtime source. The JS suite is `not run`, not passing.

The evidence limits recorded in [attempt 01](01.md) are unchanged and still
apply: one host, one OS, one version of each client; no model ran and no real
delegation was performed; the Codex vendor plugin documentation could not be
fetched; the interactive Codex TUI was not tested; the unit tests in
`tests/plugin-probes/` are fixture checks of the harness, not host evidence.

## Deviations and proposals

None. The package name is the only difference from the plan in attempt 01, and it
exists to avoid overwriting that attempt's verified archive.

## Handoff

Round base `1f7d34f1d3bf1de15c2b9cef5b3b121f2c8c27c0`; head is this attempt's
commit, reported with the package hash in the round summary. Changes stay inside
the round's write scope; `feature.json`, `reviews/`, `decisions/`, task statuses
and the feature index are untouched.

Next step is the coordinator's: review the two proposed contracts and decide
whether W14-02 implements contract 01 as written. The two questions attempt 01
flagged before a package generator is frozen — re-reading the Codex vendor plugin
documentation, and answering the TUI startup question with a PTY smoke of the
wave12 AC-08 kind — remain open and outside this round.
