# 02 — Working wave13 interface, 2026-09-16

The user explicitly delegated determination of the working interface from wave13 code and review, with exact commits and assumptions. This is not acceptance of wave13. Independent wave14 work continues; combined integration/validation follows wave13 acceptance. No further product decision is required for this interface.

Frozen references: exporter implementation `cf2365c3d473be2c7119d0682b85b70e155ec661`; independent export review `853302cdf821671020e11f2a74c2406f33a68b75`, `docs/features/F-W13-diagnostics/reviews/02-export.md` (REWORK R2-01–06). No wave13 files/worktree/runtime have been changed or merged.

Working compatibility boundary: `scripts/diagnostics/collect.mjs` collectVersions reads runtime-manifest and install selection; existing safe identity fields are runtime_id/package_version/source.commit/instructions.set_sha256. runDiagnose calls runDoctor with safeSubset and projects checks using `projectDoctorCheck`, which currently keeps id/status/code/aliased summary/next_step and drops arbitrary top-level doctor additions. Therefore doctor.distribution alone is NOT diagnose integration. Future integration must explicitly allowlist a small distribution object or compatible check fields using wave13's corrected projector. Do not embed unvalidated metadata verbatim. Prefer package name/version, canonical runtime id/commit, instruction digest and declared/applied pin; unknowns null. Keep configured and observed facts distinct. No extra logging sink, raw paths or changes to retention.

Assumptions: wave13's privacy and safe-read findings will be fixed before combined validation; final accepted commit may rename helpers but should retain these semantics. Reconcile exact accepted interface then. Until that point wave14 can implement and verify its own doctor output and retain joint diagnose/logging checks as deferred by this user instruction, not PASS.
