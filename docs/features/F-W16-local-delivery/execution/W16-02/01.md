# Ledger W16-02 / attempt 01 — canonical instructions and generated distribution

## Scope / Inputs

Bridge round for W16-02 only, executed under the pinned runtime `0.3.2-34ecb8d45465`
instructions (round package still required); the new instructions written here are product
output and do not govern this round. Authority: [execution decision](../../decisions/01.md)
and plan decision `docs/plans/wave16/decisions/01.md` (local commits; no push, merge, deploy,
model smoke or delegation).

Inputs (sha256 prefix at base): contract `contracts/local-delivery.md` `33a7aa72`, contract
review `reviews/01-contracts.md` `e3aea319` (PASS; nonblocking notes on shipped resource and
dirt handling), feature decision `d3ba10cb`, plan decision `eafb400b`, plan `a4569181`,
`work-items/W16-02.md` `efeb766e`.

Contract base `3fd34f7106eadc93bda8c4a0227f49d3d87445fd`. Worktree at start: clean
(`git status --porcelain=v1 --untracked-files=all` empty), so no preexisting dirt.
No dependency install was needed: `packages:check` and both Python suites passed at base.

## Outcome

`complete`. Delivered in `05954fe` (instructions + generated plugins) and the ledger commit:

- New shipped resource `.agents/skills/feature-execute/references/local-delivery.md`
  (sha256 `bd0b7002…a341`): operational form of the reviewed contract without dependency on
  feature-development documents — why line 1 of `summary`, the `DELIVERY=local-v1` line,
  executor procedure, coordinator receipt checks, delivered-SHA review vs integration drift,
  dispositions, PARTIAL/resume/corrections/evidence, contracts that still require a package.
- Dirt handling per review note: start record (status + `git hash-object` of in-scope dirty
  paths), staging by name only, classes `preexisting` / `own` / `overlap` / `foreign`;
  `own` or `overlap` forces `OUTCOME=PARTIAL`; preexisting dirt stays separate even inside the
  scope and is never cleaned; coordinator records in-scope dirt when issuing a contract.
- `feature-execute` SKILL: executor delivers through Git and the delivery line; exports only
  when the contract requires a package (older contracts followed literally, including their
  `PACKAGE=` prefix); PARTIAL commits ledger and completed work; Finish uses a short handoff,
  ZIP only for a recipient without repository access or on request. Standalone mode unchanged.
- `bridge-loop.md`: permissions, pre-round dirt check, contract base as full SHA,
  expected deliverable, deadline wording, review steps via receipt checks and delivered HEAD,
  package verify only when required, findings, final handoff without ZIP, "continue" reading
  and intent files (namespace kept; intent never enters the round's commit range).
- `feature-exchange` SKILL: when to exchange (no repo access / explicit request);
  "Bridge rounds" section replaces mandatory round packages. `feature-review` SKILL:
  "Bridge round deliveries" with receipt checks, delivered HEAD, drift attribution.
- `docs/features/README.md`, both `using-bridge` roles (+ `references/contracts.md` note on
  `commit_or_diff`), `docs/feature-workflow.md`, `README.md`: same procedure, ZIP optional.
- Generated `plugins/**` by `npm run packages` only; marketplace files unchanged; generator
  unchanged (sibling-relative links and code-path references already resolve in the package).

Unchanged: API, state machine, version/release pin, runtime, historical ledgers/reviews/
decisions, W15 intent/replay and namespace, role split, scope/lease, user acceptance.

## Problems and resolutions

- Links from the shared guide and role skills to the new resource cannot be Markdown links:
  the generator rewrites `.agents/skills/` to `${CLAUDE_PLUGIN_ROOT}/skills/`, which breaks a
  relative link. Resolution: follow the existing `bridge-loop.md` convention (code path in the
  guide, "`feature-execute` reference `local-delivery.md`" in role skills); real relative links
  only between sibling workflow skills, which resolve both in the repo/runtime and the package.
- `test_plugin_distribution.py` asserts the helper path in the packaged `feature-execute`
  SKILL; kept via the "contract requires a package" paragraph.
- A first classification draft counted every new dirty entry as `own`; entries outside the
  write scope cannot be the executor's, so a `foreign` class was added before commit.

## Validation

Run on the committed instruction tree `05954fe` and again on the final head (see round
summary for exit codes of the final run):

- `npm run packages:check` → "packages match their canonical sources (source digest
  68f7d8fa101a)".
- `python3 -m unittest discover -s tests -p 'test_plugin_distribution.py' -v` → 15 tests OK.
- `python3 -m unittest discover -s tests -p 'test_feature_exchange.py' -v` → 29 tests OK.
- Local Markdown link check (temporary `/tmp/w16-linkcheck.py`: every relative `](…)` target
  in changed/untracked `.md` files, canonical and generated, must exist) → 20 files, 66 links,
  0 broken. Generated guide shows `${CLAUDE_PLUGIN_ROOT}/skills/feature-execute/references/local-delivery.md`.
- `git diff --check` on the range.

Not done (by contract): W16-03 regressions, real temporary-repository Git scenarios, model
smoke, full `npm test`. Text checks do not prove that a model follows the new procedure (R16-N2).

## Deviations and proposals

- Implementation choices: resource placed under `feature-execute/references/` beside
  `bridge-loop.md`; `foreign` dirt class added to the W16-01 contract's own/preexisting split
  (refines it, no behavior outside the contract); `README.md` gets one sentence only.
- Proposal for W16-03: preserve the W16-01 normalization probe as repository tests and cover
  the § 4 receipt checks and dirt classes with real temporary Git repositories.
- Residual: out-of-scope `AGENTS.md` has no ZIP obligation; `docs/HANDOFF.md` mentions packages
  only as historical status. Both were left unchanged (outside scope); the coordinator owns any
  HANDOFF update at integration.

## Handoff

Delivered head: the ledger commit on top of `05954fe`, base `3fd34f7…`. Package
`F-W16-r02.zip`, purpose `implementation-review`, exported and verified after that commit;
path, sha256 and full range in the round summary. Next: independent coordinator review of
W16-02, then W16-03.
