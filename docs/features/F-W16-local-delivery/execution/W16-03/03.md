# Ledger W16-03 / 03 — correction R02-01 (rename-safe receipt)

## Scope / Inputs

Correction round (new bridge task, same Claude session) for required finding R02-01 of
[reviews/02-implementation.md](../../reviews/02-implementation.md) (W16-03 integration review,
REWORK), under the whole-wave authority of [decisions/01.md](../../decisions/01.md). Contract base
`8bdea852de86f3eb298adbb08435cb97fab789f8`; worktree clean at start (no preexisting dirt). Pinned
runtime `0.3.2-34ecb8d45465` instructions; round package `F-W16-r04.zip`, purpose
`corrections-review`. Write scope: `.agents/skills/feature-execute/**`,
`tests/test_local_delivery.py`, `plugins/**`, this directory, `evidence/**`.
Earlier ledgers [01](01.md) and [02](02.md) are unchanged.

## Outcome

`complete`. Commit `d3b5385` fixes R02-01 in the published procedure and in the test
transcription; this ledger and the updated [report](../../evidence/W16-03-report.md) follow in a
docs commit.

- `local-delivery.md` § 4.6: endpoint and per-commit path lists use
  `--no-renames --name-only -z`; the text says why (a move from outside the scope otherwise shows
  only its destination). § 4.7: ledger add check with `--no-renames`; earlier ledgers are paths
  of `git ls-tree -r -z --name-only BASE -- <ledger dir>` touched anywhere in
  `git log --no-renames --name-only -z --format= BASE..HEAD -- <ledger dir>`; `LEDGER=none` only
  for a read-only contract with `HEAD` = `BASE`. § 2 `changed_scope` lists both sides of a move.
- `tests/test_local_delivery.py`: `receipt()` uses the same commands with NUL-separated parsing
  and an explicit `read_only` flag; six `RenameTests` regressions; the `LEDGER=none` test now
  distinguishes read-only from writable contracts; the procedure guard lists the new commands.
- Plugins regenerated by the generator (marketplace files unchanged).

No production helper, API, state machine, runtime or manager record changed.

## Problems and resolutions

- Reproduced R02-01 before fixing: with the unfixed transcription (test file sha256
  `013bb94b…1da7af`, `local-delivery.md` `c5f57708…525ec639`) 36 tests ran and 5 failed:
  outside→inside rename returned `[]` (false PASS), inside→outside and valid rename lacked the
  moved source, the earlier ledger renamed onto the new ledger returned `[]`, and an earlier
  ledger edited then restored inside the range was not flagged.
- The same class hit the ledger check, as the review expected: pathspec-limited rename
  detection turned a moved earlier ledger into an "add", so an endpoint-only `MD` check cannot
  see it; the history check over BASE ledger paths closes that and edit-and-restore together.
- My first reproduction run contained a test-setup error (missing destination directory for
  `git mv`); fixed before recording the reproduction above.

## Validation

On `d3b5385` (the tested code; the docs commit adds only this ledger and the report):

| Command | Exit | Result |
| --- | --- | --- |
| `python3 -m unittest discover -s tests -v` | 0 | 83 tests OK, 0 skipped (36 in `test_local_delivery.py`) |
| `npx vitest run claude/claude-side/src/adapters/claude-code-runner.delivery.test.ts shared/control-plane/src/feature-workflow.test.ts shared/control-plane/src/wave15-continuation.test.ts` | 0 | 3 files, 29 tests passed |
| `npm run packages:check` | 0 | source digest `828af404a292` |

On the delivered head: changed-document link check, `git diff --check`, package export and
verify (round summary). Retained, not rerun: full JS suite 582 and pilot 140 from `08ed5b2`;
since then only instruction text, generated plugins and the Python test changed.

## Deviations and proposals

None. The receipt remains a test transcription; no product helper is proposed.

## Handoff

Next: focused re-review of R02-01 and related paths (scope, ledger, `LEDGER=none`), then
integration of W16-01–03. The behavioural part stays unverified without a model smoke (R16-N2).
