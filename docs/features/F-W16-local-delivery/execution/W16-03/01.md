# Ledger W16-03 / attempt 01 — regressions, investigation and AC evidence

## Scope / Inputs

Bridge round for W16-03 only, executed under the pinned runtime `0.3.2-34ecb8d45465`
instructions (round package required); instructions edited here are product output.
Authority: [execution decision](../../decisions/01.md), plan `docs/plans/wave16.md`
(`a4569181…bacb94f6`), W16-02 review [02-implementation](../../reviews/02-implementation.md)
(`7d55935c…46223173`, PASS with W16-03 test inputs), `work-items/W16-03.md` (`072d0f99…a8f14c76`).

Contract base `d4e8b7b18631ec61789c6391399d0e747c81e900`; worktree clean at start (no
preexisting dirt). `npm ci --ignore-scripts` installed `node_modules` in this checkout only
(ignored by Git); `npm run build` built this checkout only. Baseline at the base: build exit 0,
`npm test` 37 files / 575 tests passed.

## Outcome

`complete`. Commit `08ed5b2` (tests, clarifications, regenerated plugins) plus the docs commit
with this ledger and [evidence/W16-03-report.md](../../evidence/W16-03-report.md):

- `claude-code-runner.delivery.test.ts` (`48bffefc…e5267ff2`, 7 tests) with fixture
  `scripted-claude-cli.mjs` (`abd43828…de1a378e`): W16-01 probe made permanent through the real
  runner/adapter/orchestrator/feature workflow/deliverable service, real commits in a temporary
  repository, PARTIAL-without-blocker, and PARTIAL → recovery → correction in one session.
- `tests/test_local_delivery.py` (`d2427bc1…6a7f8c19`, 30 tests): real-Git receipt matrix,
  drift, explicit export/inspect-return in the namespace, guards against drift from the reference.
- `local-delivery.md` (`c5f57708…525ec639`), `feature-execute` SKILL and `bridge-loop.md`
  clarified for findings W16-03-F1–F5 (report table); plugins regenerated by the generator.

No API, state machine, release version, runtime, manager file, historical ledger or contract
changed; no production helper added.

## Problems and resolutions

- F1: a claimed `OUTCOME=PARTIAL` without blocker is stored COMPLETE/DONE → blocker required,
  coordinator routes a correction round.
- F2: my first test draft committed with `git add -- <own>` + bare `git commit` and swept a
  preexisting staged file into the delivery → pathspec commits in executor and coordinator text.
- F3: my checker first counted paths; a rename record names two paths and `git rm --cached`
  yields two records for one path; plain porcelain quotes spaces → `-z`, record counts.
- F4/F5: preexisting path changed by the range; deleted-path fingerprints → receipt step and
  `deleted` fingerprint.
- Review input "PARTIAL without blocker / dirt edge cases" investigated as above; the adapter
  behaviour is correct per its contract (honesty gate), so the fix is in the instructions.

## Validation

Full contract checks were run on `08ed5b2` (numbers in the report) and rerun on the final
delivered head; exit codes of the final run are in the round summary. Commands:
`npm ci --ignore-scripts && npm run build && npm test`; `python3 -m unittest discover -s tests -v`;
`python3 -m unittest discover -s tools/pilot/tests -v`; `npm run packages:check`; changed-document
link check (temporary `/tmp/w16-linkcheck2.py`, relative Markdown links of every `.md` changed in
the range); `git diff --check <base> HEAD`. Fakes: synthetic CLI (no model), test-local receipt
transcription. Not run: model smoke (not authorized).

## Deviations and proposals

- Implementation choices: receipt as a test transcription instead of a production helper;
  Python for the Git matrix, vitest for the adapter path; `glob_re` ports `scope.ts`.
- Proposal for integration review: confirm F1–F5 wording; consider a real Astra–Claude smoke
  without ZIP as the only way to close the behavioural part of AC-01/02/04/05 (R16-N2).

## Handoff

Delivered head: the docs commit on top of `08ed5b2`, base `d4e8b7b…`. Package `F-W16-r03.zip`,
purpose `implementation-review`, exported and verified after that commit; path, sha256 and full
range in the round summary. Next: independent integration review of W16-01–03.
