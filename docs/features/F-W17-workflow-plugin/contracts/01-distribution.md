# W17-01 — distribution, source selection and compatibility contract

Status: proposed contract, corrected for [review 02](../reviews/02-contracts.md) R02-01
(§ 4, § 6); W17-01 gate before W17-02.
Authority: [decision 01](../decisions/01.md) (whole W17-01–04 execution, local only).
Base: `a67aee78234ac60444d9dec18ea65f20cbdd241c`; correction base
`23cfa402c9cde68d1c10c11f77817757cdda3a15`. Inputs: [brief](../brief.md),
[design](../design.md), [plan review](../reviews/01-plan.md), W17-01 work item.
Evidence: [`evidence/W17-01-host-probes.json`](../evidence/W17-01-host-probes.json)
(historical candidate run: hosts and fixtures, before R02-01) and
[`evidence/W17-01-correction-02.json`](../evidence/W17-01-correction-02.json) (fixture-only
source selection after R02-01), both from `scripts/plugin-probes/wave17_probe_distribution.py`;
fixture checks in `tests/plugin-probes/test_wave17_distribution.py`. Ledgers:
[01](../execution/W17-01/01.md), [02](../execution/W17-01/02.md).

Host versions observed: Claude Code `2.1.280`, codex-cli `0.155.1`, Node `v24.15.0`,
Python `3.12.3`. All host facts below were observed in disposable profiles with no model
turn (§ 8). Items marked **UNVERIFIED** concern model behaviour or host features no probe
could observe; they are not claimed.

This contract fixes names, package contents, the instruction-source rule and the migration.
It implements nothing in the product: generator, manifests, launcher and documentation stay
unchanged until W17-02–04.

## 1. Names

| Item | Value |
| --- | --- |
| Logical plugin and manifest `name`, both clients | `feature-workflow` |
| Codex package | `plugins/feature-workflow-codex/` with `.codex-plugin/plugin.json` |
| Claude Code package | `plugins/feature-workflow-claude/` with `.claude-plugin/plugin.json` |
| Marketplace | existing `claude-codex-bridge`: one new entry in `.agents/plugins/marketplace.json` and one in `.claude-plugin/marketplace.json` |
| Install id, both clients | `feature-workflow@claude-codex-bridge` |
| Version | `package.json` version (same release train as the bridge); `GENERATED.json` carries the source digest |
| Unchanged | `bridge-codex`, `bridge-claude`, their paths, `bridge-upgrade`, the installer and `release.json` |

The same manifest name in both clients is deliberate: Claude Code resolves a `--plugin-dir`
plugin against installed plugins **by plugin name** (observed:
`Plugin "feature-workflow" from --plugin-dir overrides installed version`), and § 6 relies on it.

```sh
codex plugin marketplace add <clone path or owner/repo>
codex plugin add feature-workflow@claude-codex-bridge

claude plugin marketplace add <clone path or owner/repo>
claude plugin install feature-workflow@claude-codex-bridge
```

## 2. The six entry points

| Skill | Claude Code (observed listed name) | Codex (observed listed name) |
| --- | --- | --- |
| design | `feature-workflow:feature-design` | `feature-workflow:feature-design` |
| plan | `feature-workflow:feature-plan` | `feature-workflow:feature-plan` |
| execute | `feature-workflow:feature-execute` | `feature-workflow:feature-execute` |
| review | `feature-workflow:feature-review` | `feature-workflow:feature-review` |
| decide | `feature-workflow:feature-decide` | `feature-workflow:feature-decide` |
| exchange | `feature-workflow:feature-exchange` | `feature-workflow:feature-exchange` |

- Claude Code lists plugin skills only as `<plugin>:<skill>` in both `skills` and
  `slash_commands` of the session init frame; explicit invocation is
  `/feature-workflow:feature-review`. No plugin produced a bare `feature-*` name in any case.
- Codex lists plugin skills as `<plugin>:<skill>` in the model-visible skills table
  (`codex debug prompt-input`), with the absolute skills root of the installed cache.
  Explicit mention `$feature-workflow:feature-review` in the interactive composer is
  **UNVERIFIED** (only the listed name was observed).
- Natural requests ("Review the plan in docs/plans/example.md") stay valid. Skill names and
  descriptions come unchanged from the canonical `.agents/skills/`; which entry a model picks
  from a natural request is **UNVERIFIED**. Plugin availability grants no authority (brief).

## 3. Package contents

Both packages, generated by `scripts/plugin-packages/generate.mjs` from `.agents/skills/` and
`docs/features/README.md` — one source, no hand-maintained copy:

| Path in package | Content |
| --- | --- |
| `skills/<six>/SKILL.md`, `agents/openai.yaml` | the six canonical skills, instruction text rewritten per client |
| `skills/feature-execute/references/bridge-loop.md`, `local-delivery.md` | both references, wave16 `local-delivery.md` included |
| `skills/feature-exchange/scripts/feature_exchange.py` | the exchange helper, byte-identical to its source |
| `workflow/README.md` | the shared workflow guide |
| `scripts/select-source.mjs` | the read-only source reader (§ 4), code copied verbatim |
| manifest, `GENERATED.json` | name/version; generator provenance and source digest |

Not in the workflow packages: `using-bridge`, `bridge-upgrade`, the installer, `bridge.mjs`,
any MCP configuration, and the test-only `tests/test_local_delivery.py:receipt()`.
No new public API or production receipt parser.

Reference rewriting:

- Claude: the existing `rewriteForClaude` (`${CLAUDE_PLUGIN_ROOT}`), unchanged.
- Codex: no plugin-root variable is documented, and expansion in skill text is **UNVERIFIED**.
  The Codex rewrite anchors checkout paths to `<package>` — the directory two levels above a
  skill's `SKILL.md`, whose absolute root the host lists — and keeps Markdown links relative
  (`](../../workflow/README.md)`). `scanForbidden` gains `<package>` as an anchor.
  Candidate: `rewriteForCodex` in `scripts/plugin-probes/wave17_fixtures.mjs`.

Observed on candidate packages built from the base: six entries in each; 36 references
checked per post-wave16 package, 0 unresolved; 0 checkout-relative references; the packaged
helper exported and verified a synthetic feature outside any bridge checkout, supplying the
guide from the installation (`integrity: ok`). Code (`.py`, `.mjs`) is never rewritten.

## 4. Instruction source selection (AC-03, AC-07)

Before phase work, every entry runs the packaged reader and follows its answer:

```sh
node "${CLAUDE_PLUGIN_ROOT}/scripts/select-source.mjs" --json          # Claude Code
node "<package>/scripts/select-source.mjs" --json                     # Codex
# add --standalone only when the user explicitly scoped the work as without the bridge
```

Output format `feature-workflow.instruction-source/v1`: `source` (`plugin` | `runtime` |
`none`), `code`, `instructions` (`workflow_skills`, `guide`, `exchange_helper`,
`local_delivery`), `runtime`, `package` (with `inside_runtime`), `next_step`, `record`,
`reads_only: true`. Exit 0 = answer, 3 = refusal (`source: none`), 1 = reader error (also a
package root that is not an existing directory).

One selection path, pin first (R02-01): the worktree and its declaration are always read and
the pin is always classified by the pinned runtime before anything else. Where the package lies
on disk is neither a pin nor a delegation identity; it is only compared with a pin that the
runtime classified as serving (last two rows).

| Worktree | `code` | Source | Rule |
| --- | --- | --- | --- |
| not a Git worktree | `STANDALONE_NO_WORKTREE` | plugin | own resources |
| no declaration, no bridge traces | `STANDALONE_NO_BRIDGE` | plugin | own resources; no MCP, runtime or setup needed |
| valid pin, runtime `ready` / `inherited-pristine` | `PINNED_RUNTIME` | runtime | read `<workflow_skills>/<skill>/SKILL.md`, runtime guide and helper; never mix with the package copy. Also for an explicitly standalone task; a narrower user instruction still bounds scope |
| pinned runtime not installed | `RUNTIME_MISSING` | none | diagnose; the bridge setup skill installs exactly that pin |
| pin state other than ready (commit mismatch, diverged, foreign/partial record, …) | `PIN_UNRESOLVED` | none | report the runtime's own `next_step` |
| declaration unreadable / no valid runtime id | `DECLARATION_INVALID` | none | diagnose |
| `enabled: false` | `PROJECT_DISABLED` | none | ask; standalone only when scoped |
| `.bridge-runtime/`, `.bridge/` or `[mcp_servers.bridge]` without declaration | `LEGACY_LAYOUT` | none | wave12 layout; migrate with bridge `status`/`setup` |
| runtime without a usable `locate.mjs` | `RUNTIME_WITHOUT_READER` | none | use the bridge plugin's `status` |
| any `none` case above with `--standalone` | `EXPLICIT_STANDALONE` | plugin | records `pin_not_used`; never a silent fallback |
| serving pin, package inside the **same** `<home>/runtimes/<pin>/` | `PINNED_RUNTIME` | runtime | `package_matches_pin: true`; the delegated runtime package works with no personal install |
| serving pin, package inside a **different** installed runtime | `PACKAGE_PIN_MISMATCH` | none | hard refusal, not waived by `--standalone` (a valid pin wins); run the pinned instructions or move the pin |

Pin classification is not reimplemented. The reader checks that the pinned runtime's
`scripts/bridge-project/locate.mjs` textually exports `status` and `instructionPaths`, imports
it and calls `status()` — the classifier used by the bridge plugin's `status` and the entry's
`--status`. Its only own logic is finding that file; its bridge-home rule is cross-checked
against the runtime's answer (`HOME_MISMATCH` refuses). The file in runtime
`0.3.2-34ecb8d45465` is identical to the base, so the current pin is supported.

An unresolved pin (missing runtime, commit mismatch even when the package lies in the runtime
with the pinned id, disabled project, legacy layout) is answered by its own row, whatever the
package location; only `--standalone` turns it into `EXPLICIT_STANDALONE`.

Read-only guarantees, each observed on fixtures: no file in the project, bridge home or package
changed across 18 fixture reads, and no file in the project or bridge home across the 18
runtime-package reads of the correction (path, SHA-256, mtime and mode snapshots); a tripwire
`.bridge-project/entry.mjs` was never executed; nothing is installed or set up; no manager is
claimed; `.bridge/` is never opened. The live worktree read (real home, current pin) answered
`PINNED_RUNTIME`, state `ready`, and left `.bridge-project/` and `.bridge-runtime/` unchanged.

Ledger record: a significant execution or review states the `record` line, e.g.
`INSTRUCTIONS=runtime:0.3.2-34ecb8d45465 SET=ecb375b5…` or
`INSTRUCTIONS=plugin:feature-workflow@0.3.3 DIGEST=…`. No per-operation registry.

## 5. Old pin, new resources

| Context | Instructions | `local-delivery.md` | Round handoff |
| --- | --- | --- | --- |
| standalone, post-wave16 package | package | yes | not a bridge round |
| project pinned to `0.3.2-34ecb8d45465` (pre-wave16) | runtime 0.3.2 | no | ZIP round package via the runtime helper, unchanged |
| project pinned to a runtime built from post-wave16 sources | that runtime | yes | `DELIVERY=local-v1`; package only on request |
| open older contract naming a ZIP (e.g. this round) | the contract | — | follow the contract literally |

Observed: two fixture runtimes extracted from `34ecb8d` and from the base differ
(`local-delivery.md` absent vs present). A pinned-0.3.2 project resolved to the 0.3.2 set
through both the 0.3.2-content and the 0.3.3-content package (same `record`), while
standalone use followed the installed package version. A plugin update therefore does not
change a pinned project's instructions or pin. Packaging must not copy package skills into a
runtime or project, alter an installed runtime, or remove the ZIP duty of a pre-wave16 pin.
W17-02 keeps `local-delivery.md` in both packages and checks its bytes against the source.
The wave16 `--no-renames -z`, add/revert, dirt-class and `PARTIAL`/blocker rules travel unchanged.

## 6. Delegated executor (AC-05)

Observed, empty profile: `--plugin-dir <runtime>/plugins/bridge-claude` lists the six
`bridge-claude:*` entries with no install. With personal installs present:

- the runtime's `bridge-claude` overrides a personally installed `bridge-claude`;
- with a pre-W17 runtime (only `bridge-claude` passed), a personal `feature-workflow` stays
  visible: the session lists `bridge-claude:*` **and** `feature-workflow:*`;
- passing both runtime directories (`--plugin-dir <runtime>/plugins/bridge-claude
  --plugin-dir <runtime>/plugins/feature-workflow-claude`) overrides both personal installs;
  every listed feature entry comes from the runtime.

Contract:

1. `<runtime>/plugins/bridge-claude` keeps its path and self-sufficient content. Installed
   runtimes and the runner's `--plugin-dir` path are not changed.
2. Runtimes built from W17-03 on also ship `plugins/feature-workflow-claude`, and the launcher
   passes it as a second `--plugin-dir` when present (same existence check as today). Both
   packages are generated from one source commit; W17-03 asserts identical feature bytes.
Source selection for a delegated round follows § 4 like every other use: the project pin is
resolved first, and a runtime-supplied package is accepted only when it lies in the pinned
runtime (`PINNED_RUNTIME`, `package_matches_pin: true`); a package from another runtime is
`PACKAGE_PIN_MISMATCH`. Verified on fixtures for the 0.3.2 and 0.3.3 fixture runtimes and read
only against the real installed `0.3.2-34ecb8d45465` package for this worktree; no delegated
host session was rerun for the correction.

3. Pre-W17 runtimes cannot change. The personal copy stays visible there; the round contract
   remains authority and the personal copy's reader answers `PINNED_RUNTIME`. That the model
   follows either is **UNVERIFIED** (§ 9, risk R1). No personal-profile change is required.
4. The executor never depends on the personal marketplace.

## 7. Legacy coexistence and migration (AC-04)

Observed: Claude with both plugins lists 12 qualified entries and no bare name; after
`claude plugin disable bridge-claude@claude-codex-bridge` only the six `feature-workflow:*`
remain. Codex `bridge-codex` contributes only `bridge-codex:bridge` and
`bridge-codex:bridge-upgrade`. Repo-local `.agents/skills/feature-*` are listed **bare**
beside plugin entries. A per-session `-c 'plugins."<id>".enabled=false'` did not hide a
plugin in `debug prompt-input`, so no Codex per-session disable is relied on.

Neither plugin declares a dependency; no host dependency resolution is assumed.
The supported personal Claude configuration is `feature-workflow` enabled and `bridge-claude`
disabled or uninstalled. The host resolves explicit qualified names but not which duplicate a
model picks for a natural request (**UNVERIFIED**), so docs require this migration.
Migration is explicit and user-run; nothing is uninstalled or disabled automatically, and
`bridge-upgrade` keeps refreshing only already installed bridge plugins.

```sh
# Claude Code — each step exited 0 in the isolated profile
claude plugin marketplace update claude-codex-bridge
claude plugin install feature-workflow@claude-codex-bridge
claude plugin disable bridge-claude@claude-codex-bridge     # or: claude plugin uninstall …
claude plugin enable bridge-claude@claude-codex-bridge      # rollback

# Codex — no disable step: bridge-codex carries no feature-* entry
codex plugin marketplace upgrade claude-codex-bridge        # Git marketplaces only; not probed
codex plugin add feature-workflow@claude-codex-bridge
```

Effects: delegated rounds are unaffected (§ 6). A disabled `bridge-claude` also hides its
Claude `using-bridge` role in native sessions. A wave12 worktree with copied repo-local skills
answers `LEGACY_LAYOUT`; bridge `setup` migrates it and keeps user `.agents/skills` files, so
stale bare copies are the user's to remove. In the bridge repository itself `.agents/skills`
are the development sources, not a pin.

Update and uninstall, observed: Claude `update` moved `0.3.2`→`0.3.3` and served
`local-delivery.md`; `uninstall` removed all entries. Codex re-`add` left only the `0.3.3`
cache (previous deleted); `remove` deleted the cache. Fixture runtimes were byte-identical
before and after every update/uninstall; runtimes live outside plugin caches.
A standalone Codex session can lose its cached files on update (W14); pinned work reads the runtime.

## 8. Host evidence and limits (AC-08 input)

Reproduce: `python3 scripts/plugin-probes/wave17_probe_distribution.py --out <file>`
(`--no-hosts` for the host-free part); `python3 -m unittest discover -s tests/plugin-probes`.

- Isolation: `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, bridge home, marketplaces and projects are
  created under `~/tmp/w17-01-probes/`; `probe_lib.assert_isolated` refuses personal
  profiles. The one live read touches only this worktree and the real bridge home, read-only.
- Claude sessions hit a local HTTP 400 stub; every result frame reported cost 0 and zero
  tokens. Codex evidence comes from `debug prompt-input`; the isolated `CODEX_HOME` had no
  credentials. Neither is a network capture.
- Instruction sets come from Git commits `34ecb8d` and the base, never from identical copies.
- Fixture runtimes stub launch binaries; they prove selection, not a working bridge.
- Correction evidence (`W17-01-correction-02.json`) is **fixture-only**: it ran the
  `--no-hosts` mode (source matrix, R02-01 runtime-package cases, package checks and one
  read-only live worktree read) and records the committed probe sources it ran from
  (`provenance`). Host versions in it are `--version` output only; no host session and no
  model ran. The host findings above come from the historical candidate run; the reader change
  does not touch host discovery, so those host cases were not repeated. That run's
  `source_selection.runtime_package` entry (`RUNTIME_PACKAGE`) records the defect R02-01
  found and is superseded by `runtime_package_cases`; the historical file is not rewritten.
- **UNVERIFIED**: model choice among listed entries; model compliance with the reader or a round
  contract over a personal copy; Codex `$plugin:skill` composer syntax; plugin-root expansion
  in Codex skill text; Git-marketplace refresh (local marketplaces were used).

## 9. Handoff to W17-02–04 and residual risks

- W17-02: add both packages and manifest entries to the generator; move the reader to a
  canonical source (e.g. `scripts/workflow-source/select-source.mjs`, copied verbatim); add
  `rewriteForCodex` and the `<package>` anchor; an entry preamble running the reader; checks
  for six entries, links, `local-delivery.md` equality, helper portability and `packages:check`.
  Carry the § 4 pin-first rule and the R02-01 matching/mismatch/unresolved cases unchanged.
- W17-03: second `--plugin-dir` (§ 6.2) with identical-bytes test; keep runner path and old
  runtimes; delegated tests with and without personal installs on the actual generated
  packages and the real launcher arguments, not only this candidate fixture layout.
- W17-04: README and `docs/plugin-distribution.md`: install, § 7 migration, § 5 matrix,
  host-vs-model limits; AC-01…08 evidence map.
- R1: delegated rounds of pre-W17 pins still see a personal `feature-workflow`
  (observed). Mitigation is text-level only (UNVERIFIED). Moving the pin removes it.
- R2: with both Claude plugins enabled, natural-request routing is model behaviour.
  Supported configuration and docs are the mitigation.

| AC | Contract | Evidence |
| --- | --- | --- |
| AC-02 | § 1–3 | Claude/Codex `workflow_only_exposes_six_qualified_entries`; package checks |
| AC-03 | § 4–5 | source matrix; `plugin_version_does_not_change_pinned_answer`; correction: `mismatched_runtime_package_refused_even_when_standalone`, `unresolved_pin_never_answered_by_package_location` |
| AC-04 | § 7 | `legacy_and_workflow_both_listed`, `disabling_legacy_leaves_only_workflow`, Codex coexistence |
| AC-05 | § 6 | delegated host cases, `new_runtime_shape_overrides_personal_workflow`; correction: `matching_runtime_package_selects_declared_runtime` |
| AC-07 | § 4, § 7 | `source_reads_wrote_nothing`, tripwire, live read, `runtimes_unchanged` |
