# Ledger W17-02 / attempt 01 — workflow packages and six entries

## Scope / Inputs

Combined W17-02→03→04 bridge round; this ledger covers W17-02. Authority:
[decision 01](../../decisions/01.md); contract gate [review 02](../../reviews/02-contracts.md)
PASS on `a943866` (R02-01 resolved). Executed under the pinned runtime `0.3.2-34ecb8d45465`
instructions (ZIP round package):
`INSTRUCTIONS=runtime:0.3.2-34ecb8d45465 SET=ecb375b53ae702c93b07b13611e284acead87e31d3496e70ae2b311fe31570ec`.

Inputs (sha256 prefix at base): brief `31a0b69b`, design `09d3e34f`, contract
`01-distribution.md` `11854632`, review 02 `806dba88`, decision `855b3778`,
`work-items/W17-02.md` `5d2bb505`, operational `local-delivery.md` `58504b2e`.
Base `9e7e9c0151d666146f6b8d51ee712f1c723c3a60`; worktree clean at start. `node_modules` was
absent and was installed with `npm ci --ignore-scripts` (git-ignored).

## Outcome

`complete` in `08cefbe`:

- Canonical reader `scripts/workflow-source/select-source.mjs` (the corrected W17-01
  selector with an exported `main`); `scripts/plugin-probes/wave17_select_source.mjs` now only
  re-exports it, so no second algorithm remains. Fixtures copy the canonical file.
- `scripts/plugin-packages/generate.mjs`: `plugins/feature-workflow-codex` and
  `plugins/feature-workflow-claude` (plugin `feature-workflow`) from the six canonical skills,
  the guide, the reader and `templates/workflow-source-preamble.md`; `rewriteForCodex`
  (`<package>` anchor, also accepted by `scanForbidden`); Codex `openai.yaml` mentions qualified;
  a separate `workflowSourceDigest` (bridge package digests unchanged); both marketplaces list
  the plugin; `--check` covers all four packages.
- Each packaged entry opens with the preamble: run the reader, follow `plugin` / `runtime` /
  `none`, record the line; the executor package and the runtime's `.agents/skills` stay canonical.
- `local-delivery.md` ships byte-identical; bridge packages keep `bridge-upgrade` and the
  installer; workflow packages carry no installer, role, MCP server or receipt helper.
- `tests/test_workflow_distribution.py` (14 tests) on the actual generated packages.

## Problems and resolutions

- The template's trailing blank line failed `git diff --check`; the separator moved into the
  generator. Adding the reader to the shared digest changed bridge `GENERATED.json`; reverted
  to a workflow-only digest so bridge packages stay byte-identical.

## Validation

At `2115018` (final code; see [W17-04 ledger](../W17-04/01.md) for the full set): `npm run
packages:check` PASS; `tests.test_workflow_distribution` 14/14 inside the 100-test Python
suite (host listing tests ran, zero model usage); probe suite 37/37 on the generated packages.

## Deviations and proposals

Choices: preamble injected by the generator rather than edited into canonical skills (keeps the
runtime's own copy from pointing onward); version from `package.json` (0.3.2 release train).
No brief criterion changed.

## Handoff

Commit `08cefbe`; W17-03 uses these packages. Remaining: independent review.
