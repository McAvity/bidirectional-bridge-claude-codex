# Ledger W17-03 / attempt 01 — coexistence, delegation and migration

## Scope / Inputs

Combined round, W17-03 part, after W17-02 (`08cefbe`). Authority, runtime instructions and
base as in [W17-02 ledger](../W17-02/01.md); `work-items/W17-03.md` `7556efe1`; contract § 6–7.

## Outcome

`complete` in `606d64b`, `d533651`, `4ec57ca`:

- `scripts/native-bridge-mcp.mjs`: exported `EXECUTOR_PACKAGES` / `executorPackages(root)`;
  the runner gets `bridge-claude` as `pluginDir` and, when the runtime ships it,
  `feature-workflow-claude` in the new `pluginDirs`. Path and content of `bridge-claude`
  unchanged; installed runtimes untouched.
- `claude-code-runner.ts`: `pluginDirs` and `executorPluginDirs()` (order kept, duplicates and
  empty entries dropped); single `pluginDir` behaves exactly as before.
- Tests: runner argv (5), real launcher with a stand-in `claude` on PATH for both the repository
  layout (two `--plugin-dir`) and an older layout without the workflow package (one) (3);
  real runtime installed from HEAD in a disposable home: launcher lists both packages, the
  runtime's workflow package selects its own pin with the real instruction digest, the
  marketplace copy resolves the same pin, a delegated Claude session with a personal
  `feature-workflow` gets the runtime copies (override logged, zero usage) (3).
- Probe moved to the generated packages and a W17-03-shaped fixture runtime; host evidence
  [W17-03-host-probes.json](../../evidence/W17-03-host-probes.json) (`9a4dd6c5`, provenance
  `d533651`, sources committed): all fixture, Claude and Codex findings true, including legacy
  coexistence, disabling `bridge-claude`, update/uninstall with runtimes unchanged and the
  delegated case using the real launcher's package list.

## Problems and resolutions

- The older-version plugin candidate had the same version as the generated package, which a
  host update would treat as unchanged; the candidate is now `0.3.1`.

## Validation

At `2115018`: `npm test` 590/590 (includes the 8 new TS tests), Python 100/100 (includes
`InstalledRuntimeWorkflow` 3/3), probes 37/37. Host sessions model-free.

## Deviations and proposals

Migration is documentation plus host-verified commands; nothing disables or uninstalls a plugin
automatically. Pre-W17 runtimes (0.3.2) still show a personal `feature-workflow` in delegated
rounds — unchanged and documented (contract R1).

## Handoff

Commits above; W17-04 documents and validates the integrated result.
