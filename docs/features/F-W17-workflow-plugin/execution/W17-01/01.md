# Ledger W17-01 / attempt 01 — distribution and compatibility contract

## Scope / Inputs

Bridge round for W17-01 only, executed under the pinned runtime `0.3.2-34ecb8d45465`
instructions (feature-execute round executor, ZIP round package required). The new
workflow rules described in the contract are proposed product behaviour and do not govern
this round. Authority: [decision 01](../../decisions/01.md) (W17-01–04, local commits; no
push, merge, deployment, model pilots, personal profile changes or runtime rebuild/switch).

Inputs (sha256 prefix at base): brief `31a0b69b`, design `09d3e34f`, plan review
`fbb98c74` (PASS; R01-N1…N3), decision `855b3778`, `work-items/W17-01.md` `c4b49c09`,
`docs/plans/wave17.md` `4d0bfeba`. Also read: AGENTS.md, generator, `locate.mjs`, Codex
entry template, launcher `pluginDir`, Claude runner, `docs/plugin-distribution.md`,
operational `local-delivery.md`, existing `scripts/plugin-probes/`.

Base `a67aee78234ac60444d9dec18ea65f20cbdd241c`. Worktree at start: clean
(`git status` empty), so no preexisting dirt. Instructions for this round:
`INSTRUCTIONS=runtime:0.3.2-34ecb8d45465 SET=ecb375b53ae702c93b07b13611e284acead87e31d3496e70ae2b311fe31570ec`
(observed by the prototype reader on this worktree).

## Outcome

`complete`. Delivered in `bcb2ad8` and this ledger commit:

- [contracts/01-distribution.md](../../contracts/01-distribution.md) (`1007050f`): names
  (`feature-workflow`, `plugins/feature-workflow-{codex,claude}/`, one entry per existing
  manifest), six qualified entries, package contents, read-only source-selection rule and
  codes, 0.3.2 ZIP pin vs wave16 local-v1 matrix, delegated executor, legacy `bridge-claude`
  migration commands, host limits, W17-02–04 handoff and AC map.
- Probes (research instruments, not product): `scripts/plugin-probes/wave17_select_source.mjs`
  (prototype reader), `wave17_fixtures.mjs` (runtimes and candidate packages from Git commits
  `34ecb8d` and base), `wave17_probe_distribution.py` (matrix, packages, both hosts).
- [evidence/W17-01-host-probes.json](../../evidence/W17-01-host-probes.json) (`c798e474`),
  home path redacted; `tests/plugin-probes/test_wave17_distribution.py` (11 fixture tests).

Key observed host facts (Claude Code 2.1.280, codex-cli 0.155.1): both hosts list plugin
skills only as `<plugin>:<skill>`; Claude `--plugin-dir` overrides a same-name installed
plugin; delegated sessions of a pre-W17 runtime also list a personal `feature-workflow`;
passing a second runtime `--plugin-dir` overrides it; disabling `bridge-claude` leaves only
the workflow's six; Codex `bridge-codex` adds no `feature-*`; repo-local skills are listed bare;
per-session Codex `-c plugins."…".enabled=false` did not hide a plugin; update/uninstall
never touched fixture runtimes.

## Problems and resolutions

- The contract's `--purpose implementation` is not a value of the pinned helper
  (`plan-review|contract-review|implementation-review|decision|corrections-review`). Used
  `implementation-review`, the pinned skill's value for new round scope.
- First Codex run checked the updated cache after `plugin remove` had deleted it (false
  negative). Fixed ordering; added `remove_deletes_plugin_cache`. A constant "model-free"
  finding was moved to a method note so every finding is observed.

## Validation

On `bcb2ad8` (probe, fixtures, tests, contract, evidence):

- `python3 scripts/plugin-probes/wave17_probe_distribution.py --out …/W17-01-host-probes.json`:
  exit 0; 26/26 findings true; every Claude session cost 0 with zero tokens against a local
  400 stub; Codex via `debug prompt-input` in a credential-free `CODEX_HOME`.
- `python3 -m unittest discover -s tests/plugin-probes -v`: 30 passed (11 new, 19 existing).
- `python3 -m unittest tests.test_plugin_distribution`: 15 passed.
- `node scripts/plugin-packages/generate.mjs --check`: packages unchanged and match sources.
- `node --check` for both `.mjs`, `py_compile` for the probe; `git diff --cached --check` clean.

Fakes/stubs: fixture runtimes stub launch binaries (selection only, not a working bridge);
candidate packages are probe-built, not generator output. Not run: full `npm test`, build and
`tools/pilot` suites (no product code changed). **UNVERIFIED** and not claimed: model choice
among listed entries; model compliance with the reader/round contract; Codex `$plugin:skill`
composer syntax; Codex plugin-root expansion; Git-marketplace refresh.

## Deviations and proposals

Implementation choices: same manifest name in both clients; reader imports the pinned
runtime's own `locate.mjs` instead of running the project entry; Codex references anchored
to `<package>`. Proposals for W17-02/03 (contract § 9): canonical reader source,
`rewriteForCodex`, second executor `--plugin-dir` in new runtimes. No brief criterion
weakened. Residual risks R1 (pre-W17 delegated rounds see a personal copy) and R2 (natural
routing with both Claude plugins) are model-behaviour limits, stated in the contract.

## Handoff

Final code/evidence snapshot `bcb2ad8`; this ledger follows it. Round package exported
from base `a67aee7` with the pinned helper. Next: independent local contract review of
`contracts/01-distribution.md`, then W17-02. Nothing pushed; no runtime, profile or
coordinator file changed.
