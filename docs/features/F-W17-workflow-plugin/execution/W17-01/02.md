# Ledger W17-01 / attempt 02 — correction R02-01 (pin before package location)

## Scope / Inputs

Correction round for [review 02](../../reviews/02-contracts.md) finding R02-01 only (sha256
prefix `549f4c62`), under [decision 01](../../decisions/01.md). Contract/prototype only; W17-02
not started. Executed under the pinned runtime `0.3.2-34ecb8d45465` instructions (ZIP
round package, purpose `corrections-review`). Instructions of this round:
`INSTRUCTIONS=runtime:0.3.2-34ecb8d45465 SET=ecb375b53ae702c93b07b13611e284acead87e31d3496e70ae2b311fe31570ec`.

Base `23cfa402c9cde68d1c10c11f77817757cdda3a15`; worktree clean at start. Ledger
[01](01.md) and `evidence/W17-01-host-probes.json` are left unchanged.

## Outcome

`complete`. Delivered in `340aefa` (reader, probe, tests, contract), `0afbbbd` (evidence)
and this ledger commit.

- `scripts/plugin-probes/wave17_select_source.mjs`: the package-location shortcut
  (`RUNTIME_PACKAGE`) is removed. There is one selection path: worktree → declaration → pinned
  runtime's own `locate.mjs:status()`. The package's runtime directory (`package.inside_runtime`)
  is only a location. It is compared with a pin the runtime classified as serving: same runtime →
  `PINNED_RUNTIME` with `package_matches_pin: true`; another runtime → `PACKAGE_PIN_MISMATCH`, a
  hard refusal that `--standalone` cannot waive. A package root that is not an existing
  directory is a reader error (exit 1). No new runtime API; no ZIP requirement relaxed.
- Contract [01-distribution.md](../../contracts/01-distribution.md) (`11854632`): § 4 pin-first
  rule and two new rows, § 6 delegated selection, § 8 correction-evidence limits and superseded
  historical entry, § 9 W17-02/03 obligations (incl. the review note on generated packages and real
  launcher arguments), AC map.
- Regression cases (`runtime_package_matrix`, 19 cases incl. `--standalone` variants):
  old/new matching, old-pin+new-package and new-pin+old-package mismatch, unresolved pins
  (missing runtime, commit mismatch with a same-id package, disabled, legacy layout), no bridge,
  nonexistent root beneath a runtime. Each read is snapshotted (project + bridge home) and
  tripwired; 7 new tests in `RuntimePackageNeverOutranksThePin`.
- [evidence/W17-01-correction-02.json](../../evidence/W17-01-correction-02.json)
  (`2ca241d8`): `--no-hosts` run, `evidence_kind` fixture-only, `provenance.head`
  `340aefa0d68bfbe1f92117c7e5b39ae99ab48c94`, probe sources committed, per-file SHA-256.

## Problems and resolutions

- R02-01 reproduced on base (reader sha256 `697d5106…`) with fixture home: this worktree
  (pin 0.3.2) plus a package beneath fixture runtime `0.3.3-a67aee78234a` → exit 0,
  `RUNTIME_PACKAGE`, `declaration: null`, `SET=unknown`, new local-delivery. The same held for a
  nonexistent path beneath that runtime and for a project whose pinned runtime was missing.
- After the fix, the same commands: `PACKAGE_PIN_MISMATCH` (exit 3, declaration 0.3.2 read,
  runtime state `ready`); reader error for the nonexistent path; `RUNTIME_MISSING` for the
  unresolved pin. Read-only against the real home: the installed 0.3.2 runtime's
  `plugins/bridge-claude` as package root → `PINNED_RUNTIME`, `package_matches_pin: true`,
  `SET=ecb375b5…`.

## Validation

On `340aefa` (code; `0afbbbd` adds only evidence, this commit only the ledger):

- `python3 -m unittest discover -s tests/plugin-probes -v`: 36 passed (17 W17-01, 19 existing).
- `python3 scripts/plugin-probes/wave17_probe_distribution.py --no-hosts --out …/W17-01-correction-02.json`:
  exit 0; 10/10 findings true, incl. `runtime_package_cases_as_contracted`,
  `matching_runtime_package_selects_declared_runtime`,
  `mismatched_runtime_package_refused_even_when_standalone`,
  `unresolved_pin_never_answered_by_package_location`, `source_reads_wrote_nothing`.
- `node --check` reader; `py_compile` probe; `git diff --cached --check` clean per commit.

Fixtures, not hosts: fixture runtimes stub launch binaries; candidate packages are probe-built.
No Claude/Codex host session and no model ran in this round; the reader change does not touch
host discovery, so host cases were not repeated. Model compliance remains **UNVERIFIED**.

## Deviations and proposals

Implementation choice: a mismatched package is refused rather than silently redirected to
the pinned runtime, because the running copy then conflicts with the pin. The reviewer's
nonblocking notes (qualified entries, explicit legacy disable/uninstall) were already in
§§ 2 and 7; the W17-03 generated-package/launcher note was added to § 9. No brief criterion
changed.

## Handoff

Final code snapshot `340aefa`; evidence `0afbbbd`; this ledger follows. Round package
`F-W17-round-2.zip` (purpose `corrections-review`, base `23cfa40`) exported with the pinned
helper. Next: focused re-review of R02-01 and the related source-selection cases, then W17-02.
Nothing pushed; no runtime, profile, coordinator record or task file changed.
