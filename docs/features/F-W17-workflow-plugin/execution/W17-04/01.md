# Ledger W17-04 / attempt 01 — integration, documentation and validation

## Scope / Inputs

Combined round, W17-04 part, after W17-03. Authority, runtime instructions and base as in
[W17-02 ledger](../W17-02/01.md); `work-items/W17-04.md` `6613f863`. Independent review is the
coordinator's; nothing here is a review verdict or user acceptance.

## Outcome

`complete` in `2115018` and this ledger commit:

- README: separate install for `feature-workflow` in both clients, qualified invocation,
  pin-first behaviour and old-pin ZIP compatibility, explicit migration from `bridge-claude`,
  model-routing limit; documentation map entry.
- `docs/plugin-distribution.md`: package table, install, the workflow plugin section (entries,
  reader and codes, old pins, delegated executor, coexistence/migration, update/removal, host vs
  model limits). `docs/setup.md` needed no change.
- `scripts/plugin-probes/wave17_doc_links.py`: read-only link check per revision.
- [Evidence report](../../evidence/W17-04-report.md) mapping AC-01…08 to evidence and limits.

## Problems and resolutions

- No documentation checker existed in the repository; the recorded baseline came from an ad
  hoc home-directory substring rule. The new check reports both a strict absolute-path rule and that
  loose rule; at base and head, `docs/` gives exactly the two recorded wave16 lines, and no
  broken links exist. Historical records were not edited.

## Validation

All on `2115018896f6b6f251dc1bde2ea7fb80ae0cce41`, clean tree, exit codes observed:

- `npm ci --ignore-scripts` 0; `npm run build` 0; `npm test` 0 (40 files, 590 tests);
  `python3 -m unittest discover -s tests -v` 0 (100, none skipped);
  `python3 -m unittest discover -s tools/pilot/tests -v` 0 (140);
  `python3 -m unittest discover -s tests/plugin-probes -v` 0 (37); `npm run packages:check` 0.
- `wave17_doc_links.py --rev HEAD [--prefix docs/]`: findings identical to base `9e7e9c0`.
- Host evidence: [W17-03-host-probes.json](../../evidence/W17-03-host-probes.json), model-free.

Not run: model pilots/smoke (not authorized), publication, Git-marketplace refresh. Model
routing and compliance remain **UNVERIFIED**. The supervising runtime was not rebuilt, edited or
switched; the runtime built by the Python host tests lives in a disposable home.

## Deviations and proposals

None to the brief. The release descriptor still pins `34ecb8d`; publishing a release with these
packages and moving project pins are separate, user-authorized actions.

## Handoff

Tested snapshot `2115018`; report and ledgers follow. Round package `F-W17-round-3.zip`
(purpose `implementation-review`, base `9e7e9c0`) exported with the pinned 0.3.2 helper.
Next: independent review of the integrated W17-02–04 delivery.
