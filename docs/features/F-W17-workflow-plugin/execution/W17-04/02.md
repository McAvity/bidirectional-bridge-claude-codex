# Ledger W17-04 / attempt 02 — documentation correction (R03-01, R03-N1)

## Scope / Inputs

Documentation-only correction round for [review 03](../../reviews/03-implementation.md) under
[decision 01](../../decisions/01.md). Write scope: `docs/plugin-distribution.md`, the
[W17-04 report](../../evidence/W17-04-report.md) and this ledger. No product source, test,
runtime or coordinator file changed; [ledger 01](01.md) and all earlier ledgers and evidence are
preserved unchanged. Executed under the pinned runtime `0.3.2-34ecb8d45465` instructions (ZIP
round package, purpose `corrections-review`). Base `b093faf606d33e9c7b234a57077a013ee2de53f8`,
clean at start.

## Outcome

`complete`:

- **R03-01**: the source table in `docs/plugin-distribution.md` now matches contract § 4 and the
  canonical reader. `STANDALONE_NO_BRIDGE` requires no declaration **and** no bridge traces;
  traces without a declaration are `LEGACY_LAYOUT`; the refusal row names each code; the
  `--standalone` row waives only those refusals; `PACKAGE_PIN_MISMATCH` is a separate hard refusal
  that `--standalone` does not waive. Checked against `scripts/workflow-source/select-source.mjs`
  (`refuse()` for every waivable code, a direct `none` answer for the mismatch, trace list
  `.bridge-runtime`, `.bridge`, `[mcp_servers.bridge]`) and the existing selector tests
  (`test_broken_pins_refuse_without_fallback`, `test_old_pin_with_new_package_is_refused_not_upgraded`).
- **R03-N1**: correction of ledger 01. Ledger 01 states that no documentation checker existed.
  That is false: `docs/tools/check-doc-links.mjs` is tracked (named in `docs/release-policy.md`
  and `docs/troubleshooting.md`), the coordinator ran it at base and head, and its two historical
  findings were part of the round contract. The report now names that gate, its exit 1 and the
  baseline comparison; the W17-04 probe checker is described as supplemental.

## Problems and resolutions

- My earlier search for a checker excluded `docs/`, so I missed the tracked gate. Its
  absolute-path rule is also what flags `wave16-progress.md:113`, not only the looser rule that
  ledger 01 described.

## Validation

- `node docs/tools/check-doc-links.mjs` on full-tree `git archive` extracts: exit 1 at
  `9e7e9c0`, at base `b093faf` and at this correction's tested commit, each with exactly the two
  historical wave16 findings (`W16-01/01.md:57`, `wave16-progress.md:113`); no new problem. The
  gate was not modified.
- `git diff --check` on the round range; scope, history and prior-ledger checks; round package
  verify (see handoff).
- No product suites rerun: prose only, as review 03 allows; product checks of ledger 01 on
  `2115018` stand.

## Deviations and proposals

None.

## Handoff

Round package `F-W17-round-4.zip` (purpose `corrections-review`, base `b093faf`) exported and
verified with the pinned 0.3.2 helper. Next: focused re-review of R03-01 and R03-N1.
