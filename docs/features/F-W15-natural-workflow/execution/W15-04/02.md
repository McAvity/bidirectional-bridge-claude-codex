# Ledger W15-04 / attempt 02 — report correction and revalidation after W15-I8

## Scope / Inputs

Refresh of the feature report after the single code correction of this round, recorded in
[W15-02/03.md](../W15-02/03.md). Authority: [decisions/01.md](../../decisions/01.md); the latest
section of [reviews/02-implementation.md](../../reviews/02-implementation.md), read at this round's
base `9cf439d42a4454688e553860265540d2819ad737`. Attempt 01 is preserved unchanged; what it said
imprecisely is corrected here rather than by editing it.

## Outcome

`complete`.

**Correction: "no model ran in any round" was imprecise, and it is now wrong to repeat it.** What
is true is narrower and needs saying exactly: **no test in this wave exercises a model, and the
smoke with models remains unordered** — every check reported is model-free. That is a statement
about the evidence, not about how the work was produced. The implementation rounds were carried
out by Claude Code through the bridge, and the coordinator reviewed them; this wave has plenty of
agent activity and no agent-behaviour *measurement*. `evidence/final-report.md` now says this in
limitation 1 and in the AC-01 cell, which previously ended with a bare "no model ran". Attempt 01
of this ledger and every earlier ledger keep their original wording; this entry is the correction.

**The report now covers W15-I8.** AC-03 records the new refusal and how it was proved; AC-08
records that `setup --with-preference` against such a runtime is refused by name before any write
and moves no pin. Limitation 6 previously said `RUNTIME_WITHOUT_STATUS` had not been checked
against a genuinely older runtime — that is no longer true, since the I8 regression builds one and
shows its entry point answering with no status JSON. It is replaced by the limitation that does
apply: the precondition is a source check of the target, so a future runtime implementing the mode
under different names would be refused as unsupported, which is the safe direction.

**Proposed code pin, updated.** `756ada274fcba8c1ceabcebafae35159c3c7e722` — the commit carrying
the W15-I8 fix, its regression, the regenerated packages and the documentation; everything after it
in this round is ledgers. As before this is a proposal, not an action: `release.json` is unchanged,
no pin moved, and this worktree's own `.bridge-project/`, `.codex/config.toml` and
`.bridge-runtime/` were not touched. The active installed runtime `0.2.0-ff225e550966` was read —
it is the runtime the reported case names — and neither rebuilt nor switched.

## Problems and resolutions

The full suite had to run again in this round because `scripts/setup/` changed, not only tests. It
is re-run, not cited.

The coordinator's additional evidence in the latest review — the reproduction of the I8 case
(`ok=true`, `applied=true`, an `AGENTS.md` naming `entry.mjs --status`, and that entry ending at
EOF with exit 0 and empty stdout) — is **retained evidence for why this correction exists**. This
round did not reproduce it by hand; it encoded the same situation as a regression instead.

## Validation

Every check below ran in this round and its exit code was observed.

| Check | Command | Result |
| --- | --- | --- |
| Focused W15-I8 regression | `npx vitest run scripts/bridge-project/bridge-project.test.ts -t "cannot serve it"` | exit 0 — 2 passed, 64 skipped |
| Build | `npm run build` | exit 0 |
| Full JS suite | `npm test` | exit 0 — 37 files, 565 passed, 0 failed, 287.75s |
| Package equivalence | `npm run packages:check` | exit 0 — packages match their canonical sources |
| Documentation links | `node docs/tools/check-doc-links.mjs` | exit 0 — 164 files, no broken internal links or absolute paths |
| Whitespace and diff hygiene | `git diff --check` | exit 0, no output |

Retained coordinator evidence, not re-run here: the Python suite (46 passed), the pilot suite
(140 passed), and the manual I8 reproduction described above.

## Deviations and proposals

None beyond the updated pin proposal. Only W15-I8 was fixed; no other finding, file or behaviour
was revisited.

## Handoff

Next: the coordinator's review of the W15-I8 correction and the refreshed report, then the user's
acceptance decision, which no check or verdict in this feature substitutes for.
