# Ledger W15-04 / attempt 01 — full validation and the feature report

## Scope / Inputs

W15-04 of wave15: the shared validation on the corrected code, and the report handed to the
coordinator. Executed in the same round as the C2/I6/I7 evidence corrections recorded in
[W15-03/02.md](../W15-03/02.md). Authority: [decisions/01.md](../../decisions/01.md).

Round base `145b915f6f10aaecb19c34c4659938a0fe296865`, worktree clean at start. Inputs: the wave15
plan (row W15-04 and AC-01…AC-09), the corrected `design.md`, the latest entries of both reviews,
and every ledger of this feature.

Write scope used: the three test files named in the round contract, `design.md` (one table
separator), this feature's `execution/W15-03/**`, `execution/W15-04/**` and `evidence/**`. No
product runtime semantics were changed: the only non-test code touched in this round is one
missing `|` in a documentation table.

## Outcome

`complete` for W15-04.

The report is [`evidence/final-report.md`](../../evidence/final-report.md). It records, per
acceptance criterion, whether the verdict rests on an executed check or only on text; the exact
revisions and commands; the limitations that must travel with the delivery — foremost that **no
model ran in any round**, so the agent-behaviour half of AC-01, AC-02 and AC-07 is unverified by
design; the strict no-handle recovery boundary with its exact error and what the regression does
and does not construct; a proposed **code pin only**, with the release pin explicitly unchanged;
and what remains for the coordinator and the user to settle.

Three of the nine criteria are `partially verified` and one is `unverified as agent behaviour`.
That is stated in the report rather than rounded up.

## Problems and resolutions

The full suite had to be re-run for real in this round, not cited: W15-I7 changed an assertion
inside it. The coordinator's earlier run (560 passed, 1 failed at `native-launcher.test.ts:405`,
37 files, 283.16s on `38dd547`) is what identified the obsolete byte-identical role contract; it
is recorded as the reason for the change and is **not** counted as a passing check here. This
round's run is green on the same 37 files with 563 passing tests, three more than the 560 that run
reported: the two cases this round added to `wave15-continuation.test.ts`, and the role assertion
that previously failed and now passes.

The Python suite (46 passed) and the pilot suite (140 passed) were executed independently by the
coordinator on areas this wave did not change. They are cited as retained evidence in the report
and in this ledger, and are explicitly **not** re-run claims.

## Validation

Every check below ran in this round, on the final commit, and its exit code was observed.

| Check | Command | Result |
| --- | --- | --- |
| Build | `npm run build` | exit 0 |
| Full JS suite | `npm test` | exit 0 — 37 files, 563 passed, 0 failed, 283.30s |
| Package equivalence | `npm run packages:check` | exit 0 — packages match their canonical sources |
| Documentation links | `node docs/tools/check-doc-links.mjs` | exit 0 — 162 files, no broken internal links or absolute paths |
| Whitespace and diff hygiene | `git diff --check` | exit 0, no output |

Retained coordinator evidence, not re-run here: Python 46 passed; pilot 140 passed, both on
unchanged inputs.

Limits, repeated here so this ledger is readable alone: no model ran; a reopened store is not a
killed process; the interruption regressions drive the control plane in process rather than
through the MCP tool layer; the native identity guards keep their own suites; and no manager code
writes intent files.

## Deviations and proposals

The only proposal is the code pin in the report, offered to the coordinator. Nothing was pushed,
merged or deployed; no runtime was switched or rebuilt; no other worktree and no operator fixture
was read or written; no index, review, decision or task status was edited.

## Handoff

Final commit and package are in this round's bridge JSON summary. Next: the coordinator's
independent review of the corrected evidence and of this report, then the user's acceptance
decision — which no check, verdict or ledger in this feature substitutes for.
