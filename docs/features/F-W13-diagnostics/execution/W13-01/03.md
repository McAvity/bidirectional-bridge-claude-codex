# Ledger W13-01 / attempt 03 — reporting-only resume

## Scope

Reporting only. The bridge rejected attempt 02's final JSON: a non-empty `reproduction_results`
requires `reproduction_snapshot` and `verification_snapshot` to each carry a **full** 40-hex commit
or 64-hex digest with distinct first hashes, and attempt 02 supplied short SHAs. No product code,
test or documentation changed in this attempt; ledgers [01](01.md) and [02](02.md) are preserved
unchanged, and the coordinator's uncommitted working files were left untouched.

Tested code is `59b3de386d2f570abb7b40480cae71d2565d7688`. The commits after it —
`e26a6d95a0d755a6845da6d3c2dfdb47a5d366d8` and this ledger — contain no code.

## Why `reproduction_results` is now empty

The R1-02 red run of attempt 02 was produced by editing one line of
`shared/control-plane/src/diagnostics-log.ts` (the two-digit `FILE_PATTERN`) in the working tree
and restoring it immediately afterwards. That pre-fix state was never committed and its file
digest was not captured before the run, so it cannot be identified honestly by a full commit or
hash now. Rather than invent an identity for it, the structured report omits
`reproduction_results` and the evidence stays here as prose:

- pre-fix pattern, reviewer's public configuration (`maxFileBytes=65536`, `maxTotalBytes=262144`,
  `maxFiles=2`, 115 rotations through `record()`):
  `npx vitest run shared/control-plane/src/diagnostics-log.test.ts -t "hundredth"` → exit 1,
  `expected 18 to be less than or equal to 3` — the same 18 retained files the review reported;
- after the fix, the same command → exit 0.

The coordinator independently reran the focused suite (30 tests) and the public-configuration
115-rotation case, which leaves 2 files at a limit of 2. That is the authoritative confirmation of
the fix; nothing in this attempt re-derives it.

## Package

Attempt 02's archive is already verified by the coordinator:
`F-W13-round-1.zip`, SHA-256 `d4caa65a89d7eea61442d55dd3b0a75885f589b2509b8bebd6d597ee32f003e3`,
range `09da710c12167037deaed395f0dfcd1863d8c7a5..e26a6d95a0d755a6845da6d3c2dfdb47a5d366d8`. It is
kept. This attempt exports the same feature, purpose and base under the new, unused name
`F-W13-round-1-report-fixed.zip`, whose head is the commit carrying this ledger, and verifies it.

## Checks

Run on the final snapshot of this attempt (code identical to
`59b3de386d2f570abb7b40480cae71d2565d7688`): `npm run build`, `npm test`, `git diff --check`, and
the export plus `verify --archive` of the new package. Results are in the round's final report.
`npm ci --ignore-scripts` was run on the same code in attempt 02 and is unchanged here; the
lockfile and `package.json` were not touched.

## Limitations

1. The pre-fix R1-02 snapshot is unidentifiable and is therefore reported as prose, not as
   structured reproduction evidence.
2. Everything from ledger 02 still stands: model-free evidence only; unauthorized calls are
   diagnosable only from the process's own stderr, which the bridge does not capture; MCP schema
   violations are not observable; a takeover ends the superseded process's log without a closing
   record; the EOF gap is unchanged; retention identifies a live writer by recorded pid; redaction
   is an allowlist plus digests, not a guarantee; the `doctor` status check stays with W13-02/03.
3. This attempt adds no evidence about the product: it only repairs the report and packaging.
