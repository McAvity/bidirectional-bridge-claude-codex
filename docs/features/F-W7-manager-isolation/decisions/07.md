# W7-ID-02 — implementacja zatwierdzonego kontraktu

Status: approved. Authority: bieżące zlecenie użytkownika po review07-contract-final.md. To zgoda na implementację przez Claude’a i zwykłe poprawki, nie odbiór featura.

Implementować contracts/identity.md revision6 (19914a0), który przeszedł review07. Wykonawca: Claude, nowa runda tego samego featura i tej samej przypiętej sesji po DONE, bez recovery. Jawne deadline_ms4500000 / max_turns200. Kontrola stanu co5min. Po dostawie: niezależne verify paczki, review kodu i testów, zwykłe poprawki w tej samej sesji. Stop przy istotnej decyzji, impasie albo nowym timeoucie; bez automatycznej nowej próby po timeout.

## Plan wykonania i kryteria

1. Zachować źródło native identity z RequestHandlerExtra._meta i adapter dokładnie0.154.0; nie czytać ID z args/environment/rollout. Walidacja subagent/guardian i brakującego/obcego contextu przed mutacją.
2. Zaimplementować kanoniczną tożsamość worktree i baz, A/B/C/L, bootstrap przy pierwszej uprawnionej operacji, brak przypisania przy starcie/odczycie, fail-closed legacy i kopii stanu. Uwzględnić partial publication, crash, dwa worktree/jedną DB i jeden worktree/dwie DB.
3. Rozdzielić read/attach/init/migration, przenieść guard i zapisy do wymaganych transakcji. Zachować role Claude/internal oraz lineage. Mutatory ogólne i recovery nie mogą omijać bindingu.
4. Zaimplementować epoki, aktywną instancję/generację, H CAS, jawny takeover i historię. Preserve running worker/session, waiting_user i wyłączność aktywnego featura.
5. Pokryć macierz contract section14 testami realnego SQLite, procesów i Git worktree oraz MCP/stdio z jawnym fixture adapter. Szczególnie rasy po precheck, restart/crash/detach, ABA, reactivation i atomic rollback. Nie zastępować testów protokołu samymi testami helperów. Zachować istniejące regresje.
6. Zaktualizować dokumentację użytkowania i instrukcje obu ról w zakresie tożsamości/worktree; nic z diagnostyki, instalatora ani utrzymania zależności. Rozdział out-of-scope kontraktu odnosił się do rundy kontraktowej; niniejsza decyzja jawnie uprawnia teraz W7-ID-02, bez zmiany zachowania kontraktu.
7. Walidacja bieżącego worktree: npm ci --ignore-scripts, npm run build, npm test, python3 -m unittest discover -s tests -v, python3 -m unittest discover -s tools/pilot/tests -v; git diff --check. Nie ujawniać operator-only fixtures pilotowi; suite narzędzi to regresja bez płatnych modeli. Nie twierdzić, że syntetyczny stdio test jest pilotem dwóch żywych modeli.
8. Nowy execution/W7-ID-02/01.md: pokrycie wymagań -> pliki/testy, polecenia i wyniki, ograniczenia i checkpoint. Lokalny commit wykonawcy i export implementation-review round-6.zip z bazą checkpointu sprzed rundy; verify exact feature/purpose/base/head. Poprzednie paczki/ledgery zachować. Koordynator osobno zapisze review, statusy i punkt wznowienia.

Runtime <operator-home>/workspace/bridge-runtime-956b171 pozostaje niezmieniany. Implementacja/build/test tylko bieżący worktree, izolowane syntetyczne bazy w temp. Bez merge/rebase/cherry-pick z feature-workflow i bez automatycznej integracji wave9. Nie pushować ani przyjmować featura. Bieżący czysty checkpoint przed decyzją: da5fc87.
