# R13-01 — worktree-specific exchange namespace

Status: approved. Authority: user correction request responding to q-03; answer recorded by bridge_feature_answer_user. No feature acceptance. Review13 supplied by independent coordinating Astra is recorded unchanged, separately from the executor correction.

## Plan and acceptance

1. Deterministic namespace under ~/tmp/bridge-exchange/<worktree-key>/. Key aligns with current workspace identity: ws_ + first16hex SHA256(UTF-8 canonicalroot + NUL + canonical per-worktree gitdir). Never branch name, shared git-common-dir alone, feature ID or native manager/session ID. Resolve --repo actual Git toplevel/gitdir, symlink aliases canonicalize. Namespace selection is read-only and does not claim ownership/create bridge state. Move changes namespace; historical content is not relocated.
2. Packages in namespace/packages/, incoming/retained copies in namespace/incoming/ as needed, staging in namespace/staging/<unique-return-name>. Provide a small canonical CLI helper or automatic named output/staging options so agents do not hand-roll identity. Keep existing explicit --output/--staging paths literal under documented override policy; no redirect, overwrite or rename of historical artifacts. Exclusive creation remains. Namespace outputs/staging must stay outside repository, fail clearly if default location violates that. No installer or exporter redesign.
3. Apply same policy to feature-exchange instructions/examples, feature-execute bridge contracts, both using-bridge mirrors and current identity/user documentation/contract13. Historical ledgers/reviews/decisions and archives are immutable; mark prior flat-path examples historical by current policy, not rewriting history. Fixed round contracts must use the resolved worktree namespace.
4. Regression with TWO REAL Git worktrees, SAME feature/purpose/run filenames: both generated exports coexist in different namespace directories and independently verify against OWN source range. Same return/staging name also yields separate directories; inspect-return never overlays either repo. Verify stable identity across branch/detached changes and symlink/subdirectory invocation as appropriate; different root/gitdir differs. Preserve explicit custom output/staging behavior and rejection of existing targets. Keep tests finite/synthetic, no model.
5. Validation: python3 -m unittest discover -s tests -v (including new two-worktree regression); npm run build; npm test; python3 -m unittest discover -s tools/pilot/tests -v; git diff --check. Inspect documents for internally consistent current commands. Local executor commit and namespace-scoped round8 corrections-review ZIP, verify exact feature/purpose/base/head. New W7-ID-02 ledger06, no edits to earlier ledgers. Coordinator independent re-review and separate checkpoint.

Existing authority continues: Claude same pinned native session, new round after DONE,4500000ms/200turns, polling5min. Runtime <operator-home>/workspace/bridge-runtime-956b171 unchanged; no feature-workflow/wave9 merge/rebase/cherry-pick, no diagnostic/installer work, no acceptance.

Current namespace ws_<historical-worktree-hash>; round8 target <operator-home>/tmp/bridge-exchange/ws_<historical-worktree-hash>/packages/F-W7-manager-isolation-round-8.zip. Coordinator computed key from real current workspace before delegation. Historical archive SHA snapshots retained privately for after-work equality check; no modifications authorized.
