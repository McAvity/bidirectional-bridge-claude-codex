# Review08 — partial W7-ID-02: REWORK

Task task_<historical-5> BLOCKED/PARTIAL, attempt0, same Claude handle retained (not published). Base511d7de6fe388113e13558840ac747f1e7045834, partial commitf9bfcce59b5a87793ec83c655ba0da93bb1922c1. Coordinator independent of executor, with read-only identity_review Codex checking entrypoints. Authority decisions/07.md. Not acceptance.

Independent archive verify PASS: round-6.zip SHA b9ad74c496a60bf12fe98963b302f5ebd8b7ead357caa23af2e5e3d6b987b9a3, exact purpose/feature/base/head, clean export,24changes. The archive exists despite PARTIAL; preserve it as historical partial delivery, not completed implementation. Independent npm test -- --reporter=dot:362/362,24files PASS. Passing tests miss core contract cases below. Worker reports build/Python19PASS, pilottools110 with1failure; these latter suites not independently repeated yet.

## Required ordinary in-scope fixes

R08-01 (blocking): identity-runtime.runMutationAsync commits binding/guard before async operation. Add request-scoped immutable authority guard at actual Orchestrator delegate/prepareRecovery and FeatureWorkflow reservation transaction, before task/attempt/lease/event/idempotency writes and worker launch. Invalid first asynchronous operation must leave no ownership; takeover after precheck must deny reservation. Keep callbacks for already-launched workers independent. Test deterministic gated takeover and invalid first async call through public MCP.

R08-02 (blocking): initialize still executes DDL/migrate/schema_meta/journal in SqliteStateStore constructor BEFORE runBootstrap/runBound transaction. In bound old-schema case a foreign caller can mutate before rejection. Split nonwriting open from initialize/migrate statements; guard and transactional schema writes as contract6.3. Existing journal mode must remain unchanged. Test actual bound migration, pause/precheck takeover, no durable changes on denial, rollback schema changes on failed first operation, newer-schema refusal.

R08-03 (blocking): prepareRead activates readonly on shared cp; ControlPlane.activate closes/replaces writer and services. Polling while adapter is gated breaks its completion/telemetry/lease writes. Use separate read connection/projection without changing active writer. Test real MCP gated async call, concurrent reads/status, then successful completion and lease release, including takeover while worker runs.

R08-04 (required): missing §8 launch/recovery prohibition for public Claude. identity-runtime role!==codex accepts all mutate, including async tools. Enforce actual named operations delegate/feature_run/bothresume before side effects, while preserving permitted Claude task writes without Codex meta and trusted internal callbacks. Test valid lineage, not a rejection that only occurs because role already fails another rule.

R08-05 (required): implement explicit legacy adoption flags, provenance/events and migration; active_feature_id claim/release and feature workspace/epoch/round_launches; feature read metadata/status required by contract. No silent downgrade of these admitted gaps. Test twofeatures conflict, accept frees slot, legacy history default refuse, authorized adoption plus transactional failure, waiting_user restart and next same-worker-session round while another worktree runs.

R08-06 (required): manager-registry.detach does not bump generation or emit event, so old H CAS remains valid. Bump generation and record manager.detached exactly once; all transition events required by contract should exist. Test old CAS rejection, fresh adoption, repeat/stale-instance close no-op.

R08-07 (blocking): bootstrap publication/crash protocol incomplete. publishReservation always EEXIST-refuses existing reserving A/B, so partial publication/no-C recovery cannot succeed. published flag is set only after both links, so failure after A leaves it indefinitely. Catch around completePublication releases reservations even AFTER main commit; preserve committed C and recoverable markers on postcommit failure. assertProbeUsable never compares A/B workspace_id/nonce against C or C.database_path; recheck fresh A/B/C under lock/transaction and reject contradictions rather than repairing/ignoring. Implement all partial publication/crash cases and nonce check from contract. Parsed temp recognition currently accepts any basename with root/nonce/state only, omits git_dir and full record validation; limit to well-formed protocol records. Test killed/crash seams before/after each publication/commit, foreign/inconsistent owner/marker, shared DB/worktree races. No PID guesses or arbitrary cleanup.

R08-08 (required): complete contract section14 matrix via actual production entrypoints; current20tests don't cover the above or two stdio pairs with waiting_user restart. Keep tests finite/synthetic and clearly labelled. Review supplied compatibility mode carefully: production MCP must not silently bypass identity; legacy injected unit-store mode must be explicitly scoped and documented, not a public arg escape.

## Separate test-harness scope blocker

R08-09: tools/pilot/rework/operator/dryrun.mjs calls client.callTool without host metadata. Minimal identified correction is synthetic params._meta on its call wrapper (threadId/session_id/thread_id=dryrun-manager, codex_version0.154.0), explicitly test-only. Never weaken production metadata enforcement. That operator file is outside the existing child scope, which resume does not expand. Do not edit it from this child or inspect other operator fixtures. Coordinator must resolve this scope boundary separately; no test-pass claim/waiver. Preserve failing result until resolved.

## Recovery instructions and checkpoint

Continue useful authorized in-scope fixes R08-01..08 in SAME BLOCKED task/session with ordinary resume (no recover_timeout),4500000ms/200turns. New ledger02, preserve01 and historical partial ZIP. No replacement round/session, runtime mutation, merge/wave9 integration or acceptance. If R08-09 remains unresolved after completing all in-scope work, return PARTIAL with only that concrete blocker; do not claim COMPLETE with failed checks. Per bridge-loop coordinator notes remain uncommitted while this task is BLOCKED; executor must leave them untouched. Product commits stay executor-only. A completed recovery package, when legitimately possible, must use a new unique filename (round-6-recovery-1.zip) instead of overwriting historical round6.
