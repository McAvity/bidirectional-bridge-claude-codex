# Feature workflow user guide

Scope: user-facing Polish overview from idea through plan, delegated execution,
review, acceptance and separately authorized integration/publication. One Mermaid
flowchart; references to existing authoritative workflow and bridge instructions.
Branch: `docs/feature-workflow-guide`. No skill/package/runtime changes.

Validation: documentation link check PASS (212 files); git diff --check PASS.
Checked the guide against the shared workflow and coordinator/executor instructions.
Diagram uses standard
Mermaid flowchart syntax; no graphical rendering verified.
Next: local integration into feature-workflow; no publication or runtime update needed
for this documentation-only change.
